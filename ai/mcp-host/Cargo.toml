[package]
name = "mcp-host"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Model Context Protocol host: the browser exposes tools to agents (forward feature 9.2)"

[lib]
name = "mcp_host"
path = "src/lib.rs"

[dependencies]
