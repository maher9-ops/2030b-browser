# Live Captions

On-device speech-to-text for any media element (Chrome §5.16). Runs in the AI
inference process; audio frames never leave the device. Feeds the PiP caption
overlay (`ui/pip/src/captions.ts`).
