[package]
name = "redaction"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "On-device prompt/crash redaction pipeline (PII, secrets, regex)"

[lib]
name = "redaction"
path = "src/lib.rs"

[dependencies]
